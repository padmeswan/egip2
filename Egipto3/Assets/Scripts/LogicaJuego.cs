using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class LogicaJuego : MonoBehaviour
{

static string [] respuesta = { "K", "V", "cinco", "seis"};
static int n = 0; 
static AudioSource bien;
static AudioSource mal; 
static AudioSource puerta;
static GameObject. objPuerta;
static float incremento = 1.0f;
static int veces = 0; 
    // Start is called before the first frame update
    void Start()
    {
        bien = GameObject.Find("SonidoBien").GetComponent<AudioSource>();
		mal = GameObject.Find("SonidoMal").GetComponent<AudioSource>();
		puerta = GameObject.Find("SonidoPuerta").GetComponent<AudioSource>();
		obj.Puerta =GameObject.Find("Puerta");
		static bool puertaAbriendose=false;
    }

    // Update is called once per frame
    void Update()
    {
        if (puertaAbriendose && veces<90) {
		objPuerta.transform.Rotate(0.0f, incremento, 0.0f);
		veces++;
		
		}
    }
	
	public static void agregarPiedra(string nombre) {
	
	if (nombre == respuesta[n]){
	Debug.Log ("_______vas bien, pirdra correcta_____");
	bien.Play(); 
	n++;
	if n (n==4) {
	puerta.Play();
	Debug.Log("_____exito____");
	puertaAbriendose = true;
	}
	else {
	Debug.Log ("Mal, intenta de nuevo");
	mal.Play();
	n=0;
	}
	
	}
	
	}
}*/
